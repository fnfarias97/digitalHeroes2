const users = require('../data/users');
const fs = require('fs');
const bcryptjs = require('bcryptjs')

module.exports = {
    create: (req, res, next) => {

    },

    store: (req, res, next) => {
        
    },

    login: (req, res, next) => {
        
    },

    updateAvatar: (req, res, next) => {

    }
}